export { default as getPathsToResolvers } from "./get-paths-to-resolvers.js";

// const dateLike = z.union([z.number(), z.string(), z.date()]);
// const dateLikeToDate = dateLike.pipe(z.coerce.date());
