# notes

````ts
type HTMLInputTypeAttribute =
	| 'date'
	| 'time'
	| 'datetime-local'
	| 'week'
	| 'month'
	//
	| 'number'
	| 'range'
	//
	| 'button'
	| 'file'
	| 'hidden'
	| 'submit'
	| 'reset'
	| 'image'
	//
	| 'checkbox'
	| 'radio'
	//
	| 'color'
	| 'text'
	| 'password'
	| 'tel'
	| 'url'
	| 'email'
	| 'search'
	//
	| (string & {});

/**
 * @description
 * The type attribute values that display only number inputs are:
 *
 * - `number`: allows the user to enter numerical values only.
 * - `range`: creates a slider control for selecting a numeric value within a range.
 *
 * Example usage:
 *
 * ```html
 * <label for="quantity">Enter Quantity:</label>
 * <input type="number" id="quantity" name="quantity" min="1" max="100" step="1">
 * ```
 *
 * ```html
 * <label for="volume">Adjust Volume:</label>
 * <input type="range" id="volume" name="volume" min="0" max="100" step="1">
 * ```
 */
const numberInputs = ['number', 'range'] as const;
/**
 * @description
 * The type attribute values that are related to date and time inputs are:
 *
 *
 * Example usage:
 *
 * - `date`: allows the user to select a date from a calendar control.
 *
 * ```html
 * <label for="birthdate">Enter Your Birthdate:</label>
 * <input type="date" id="birthdate" name="birthdate">
 * ```
 *
 * - `time`: allows the user to select a time value, including hours, minutes, and seconds.
 *
 * ```html
 * <label for="appt-time">Choose an Appointment Time:</label>
 * <input type="time" id="appt-time" name="appt-time" min="09:00" max="17:00" step="900">
 * ```
 *
 * - `datetime`-local: allows the user to select a date and time value, including hours, minutes, and seconds, without a time zone.
 *
 * ```html
 * <label for="meeting-time">Choose a Meeting Date and Time:</label>
 * <input type="datetime-local" id="meeting-time" name="meeting-time">
 * ```
 *
 * week: allows the user to select a week and year, such as for scheduling or event planning.
 *
 * ```html
 * <label for="event-date">Select Event Week:</label>
 * <input type="week" id="event-date" name="event-date">
 * ```
 *
 * month: allows the user to select a month and year, such as for a credit card expiration date.
 *
 * ```html
 * <label for="card-expiration">Enter Credit Card Expiration Date:</label>
 * <input type="month" id="card-expiration" name="card-expiration">
 * ```
 *
 */
const dateAndTimeInputs = [
	'date',
	'time',
	'datetime-local',
	'week',
	'month',
] as const;

/*
    image: displays an image that represents a submit button, and submits the form when clicked. The image source is specified using the src attribute and can be any valid URL.

html

<input type="image" src="submit.png" alt="Submit Button">

    search: creates a text input field for entering search queries.

html

<label for="search-input">Search:</label>
<input type="search" id="search-input" name="search-input" placeholder="Search...">

    tel: creates a text input field for entering phone numbers.

html

<label for="phone">Enter Your Phone Number:</label>
<input type="tel" id="phone" name="phone" pattern="[0-9]{3}-[0-9]{3}-[0-9]{4}">

    url: creates a text input field for entering URLs.

html

<label for="website">Enter Your Website:</label>
<input type="url" id="website" name="website" placeholder="https://www.example.com">

*/

/*
The type attribute values and their functions are:

    checkbox: creates a checkbox control for selecting one or more options from a set of choices.

html

<label for="checkbox1">Option 1:</label>
<input type="checkbox" id="checkbox1" name="checkbox-group" value="option1">
<label for="checkbox2">Option 2:</label>
<input type="checkbox" id="checkbox2" name="checkbox-group" value="option2">

    radio: creates a set of radio buttons for selecting one option from a set of choices.

html

<label for="radio1">Option 1:</label>
<input type="radio" id="radio1" name="radio-group" value="option1">
<label for="radio2">Option 2:</label>
<input type="radio" id="radio2" name="radio-group" value="option2">

    color: creates a color picker control for selecting a color value.

html

<label for="color-picker">Choose a Color:</label>
<input type="color" id="color-picker" name="color-picker">

    email: creates a text input field for entering email addresses.

html

<label for="email">Enter Your Email Address:</label>
<input type="email" id="email" name="email">

    text: creates a single-line text input field for entering plain text.

html

<label for="username">Enter Your Username:</label>
<input type="text" id="username" name="username">

    password: creates a single-line text input field that hides the characters entered, useful for creating password fields.

html

<label for="password">Enter Your Password:</label>
<input type="password" id="password" name="password" placeholder="Password">

*/

/*
If you are using React.js and you have added the onChange event listener to an input element with type values of 'date', 'time', 'datetime-local', 'week', or 'month', the event.target.value will be a string containing the selected value in the format specified by the type.

    For the 'date' type, the value will be a string representing the selected date in the format yyyy-mm-dd.
    For the 'time' type, the value will be a string representing the selected time in the format hh:mm:ss.
    For the 'datetime-local' type, the value will be a string representing the selected date and time in the format yyyy-mm-ddThh:mm:ss.
    For the 'week' type, the value will be a string representing the selected week in the format yyyy-Www, where Www is the week number of the year.
    For the 'month' type, the value will be a string representing the selected month in the format yyyy-mm.
*/

/*
If you are using React.js and you have added the onChange event listener to an input element with type values of 'number' or 'range', the event.target.value will be a string representing the numeric value entered by the user.

    For the 'number' type, the value will be a string representing a numeric value. The user is allowed to enter a floating-point number, but the value will always be returned as a string.

jsx

<label htmlFor="age">Enter Your Age:</label>
<input type="number" id="age" name="age" onChange={(event) => console.log(event.target.value)} />

    For the 'range' type, the value will be a string representing a numeric value within a specified range. The user is allowed to drag a slider to select a value within the specified range.

jsx

<label htmlFor="brightness">Select the Brightness:</label>
<input type="range" id="brightness" name="brightness" min="0" max="100" onChange={(event) => console.log(event.target.value)} />

Note that the value will always be a string, even if the user enters a numeric value. If you need to perform arithmetic operations with the value, you may need to convert it to a number using the parseInt() or parseFloat() functions.
*/

/*
Make a function that transform a `Date` to the format and another one that take the format and transform it to `Date`

- For the 'date' type, the value will be a string representing the selected date in the format yyyy-mm-dd.
- For the 'time' type, the value will be a string representing the selected time in the format hh:mm:ss.
- For the 'datetime-local' type, the value will be a string representing the selected date and time in the format yyyy-mm-ddThh:mm:ss.
- For the 'week' type, the value will be a string representing the selected week in the format yyyy-Www, where Www is the week number of the year.
- For the 'month' type, the value will be a string representing the selected month in the format yyyy-mm.
*/

/*
// Transform a Date object to a string in the specified format
function formatDate(date: Date, format: string): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  const hour = String(date.getHours()).padStart(2, '0');
  const minute = String(date.getMinutes()).padStart(2, '0');
  const second = String(date.getSeconds()).padStart(2, '0');
  switch (format) {
    case 'date':
      return `${year}-${month}-${day}`;
    case 'time':
      return `${hour}:${minute}:${second}`;
    case 'datetime-local':
      return `${year}-${month}-${day}T${hour}:${minute}:${second}`;
    case 'week':
      const weekNumber = getWeekNumber(date);
      return `${year}-W${String(weekNumber).padStart(2, '0')}`;
    case 'month':
      return `${year}-${month}`;
    default:
      throw new Error(`Invalid format: ${format}`);
  }
}

// Transform a string in a supported format to a Date object
function parseDate(str: string): Date {
  const [dateStr, timeStr] = str.split('T');
  const dateParts = dateStr.split('-').map((part) => parseInt(part));
  let timeParts: number[] = [];
  if (timeStr) {
    timeParts = timeStr.split(':').map((part) => parseInt(part));
  }
  switch (dateParts.length) {
    case 2:
      return new Date(dateParts[0], dateParts[1] - 1);
    case 3:
      if (timeParts.length === 0) {
        return new Date(dateParts[0], dateParts[1] - 1, dateParts[2]);
      } else {
        return new Date(dateParts[0], dateParts[1] - 1, dateParts[2], timeParts[0], timeParts[1], timeParts[2]);
      }
    case 4:
      const [year, weekNumber] = dateParts;
      const firstDayOfYear = new Date(year, 0,
*/
````
